# Optional. Leave unset in development: Vite proxies /api to http://localhost:8080.
# Set it when the frontend is served from a different host than the backend.
# VITE_API_BASE_URL=http://localhost:8080/api
